# Настройка ЮKassa для Luminar

## 1. Регистрация в ЮKassa

1. Перейдите на https://yookassa.ru/
2. Нажмите "Подключить ЮKassa"
3. Зарегистрируйтесь (нужны данные ИП или ООО)
4. Пройдите модерацию (обычно 1-3 дня)

## 2. Получение API ключей

После одобрения аккаунта:

1. Войдите в личный кабинет ЮKassa
2. Перейдите в раздел **"Настройки" → "Интеграция"**
3. Скопируйте:
   - **shopId** (идентификатор магазина)
   - **Секретный ключ** (Secret Key)

## 3. Настройка переменных окружения

### На Vercel:

1. Откройте проект на https://vercel.com
2. Перейдите в **Settings → Environment Variables**
3. Добавьте переменные:

```
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_секретный_ключ
```

4. Нажмите **Save**
5. Сделайте **Redeploy** проекта

### Локально (для тестирования):

Создайте файл `.env` в корне проекта:

```env
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_секретный_ключ
DATABASE_URL=ваша_база_данных
JWT_SECRET=ваш_jwt_secret
ADMIN_KEY=ваш_admin_key
```

## 4. Настройка Webhook в ЮKassa

Webhook нужен для автоматической активации подписки после оплаты.

1. В личном кабинете ЮKassa перейдите в **"Настройки" → "Уведомления"**
2. Включите **HTTP-уведомления**
3. Укажите URL: `https://ваш-домен.vercel.app/api/payment/webhook`
4. Выберите события:
   - ✅ `payment.succeeded` (платёж успешен)
   - ✅ `payment.canceled` (платёж отменён)
5. Сохраните настройки

## 5. Тестирование

### Тестовые данные ЮKassa:

**Тестовая карта (успешная оплата):**
- Номер: `5555 5555 5555 4477`
- Срок: любая будущая дата (например, 12/25)
- CVC: любые 3 цифры (например, 123)
- 3-D Secure код: `12345`

**Тестовая карта (отклонённая оплата):**
- Номер: `5555 5555 5555 5599`

### Проверка работы:

1. Создайте продукт в админ-панели
2. Создайте промокод (опционально)
3. Перейдите на страницу `/buy`
4. Выберите продукт
5. Примените промокод (если есть)
6. Выберите способ оплаты "СБП / Карта"
7. Нажмите "Купить"
8. Введите тестовые данные карты
9. После успешной оплаты подписка должна активироваться автоматически

## 6. Комиссии ЮKassa

- **Для новых клиентов**: 2,6% за карты, SberPay, T-Pay, MirPay
- **Минимальная комиссия**: от 0,4% (при больших оборотах)
- **Подключение**: бесплатно
- **Абонентская плата**: нет

## 7. Проверка логов

Если что-то не работает:

1. Проверьте логи Vercel: **Deployments → Logs**
2. Проверьте логи ЮKassa: **Личный кабинет → Уведомления → История**
3. Проверьте таблицу `payments` в базе данных

## 8. Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте `.env` файл в Git
- Храните API ключи только в переменных окружения
- Используйте HTTPS для webhook URL
- Проверяйте подпись webhook запросов (опционально, но рекомендуется)

## Готово! 🎉

Теперь ваша система принимает платежи через ЮKassa с автоматической активацией подписок и поддержкой промокодов.
